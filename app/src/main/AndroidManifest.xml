<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ruben.rma.prettynotes" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/education"
        android:label="Notas"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.ruben.rma.prettynotes.MainActivity"
            android:label="Notas"
            android:configChanges="orientation|keyboardHidden|screenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.ruben.rma.prettynotes.AgregarNota"
            android:label="@string/add_nota"
            android:configChanges="orientation|keyboardHidden|screenSize">
        </activity>
        <activity
            android:name="com.ruben.rma.prettynotes.VerNota"
            android:label="@string/ver_nota"
            android:configChanges="orientation|keyboardHidden|screenSize">
        </activity>
    </application>

</manifest>
